import type { TooltipProps }      from '@atls-ui-parts/tooltip'
import type { ReactNode }         from 'react'

import { Box }                    from '@ui/layout'
import { Tooltip as BaseTooltip } from '@atls-ui-parts/tooltip'

import { baseContainerStyles }    from './styles.css.js'

export const Tooltip = ({ children, ...props }: TooltipProps): ReactNode => (
  <BaseTooltip {...props} container={<Box className={baseContainerStyles} />}>
    {children}
  </BaseTooltip>
)
