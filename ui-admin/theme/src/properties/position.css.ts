import { defineProperties } from 'rainbow-sprinkles'

export const positionProperties = defineProperties({
  dynamicProperties: {
    position: true,
    zIndex: true,
    top: true,
    right: true,
    bottom: true,
    left: true,
  },
})
