import { style } from '@vanilla-extract/css'

export const containerStyles = style({
  display: 'flex',
  position: 'relative',
  width: '100%',
  height: '100%',
  outline: 'none',
  overflow: 'hidden',
})
