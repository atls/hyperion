import { style } from '@vanilla-extract/css'

export const slideStyles = style({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  flexShrink: 0,
  width: '100%',
  height: '100%',
})
