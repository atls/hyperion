import styled         from '@emotion/styled'

import { background } from 'styled-system'
import { color }      from 'styled-system'

const LinePercentElement = styled.div(color, background)

export { LinePercentElement }
