import styled         from '@emotion/styled'
import { typography } from 'styled-system'
import { layout }     from 'styled-system'
import { space }      from 'styled-system'

const Label = styled.label(typography, layout, space)

export { Label }
