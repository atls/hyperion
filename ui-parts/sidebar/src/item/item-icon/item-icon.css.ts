import { style } from '@vanilla-extract/css'

export const itemIconStyles = style({
  height: '100%',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  flexBasis: 80,
  flexShrink: 0,
})
