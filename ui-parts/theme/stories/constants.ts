import type { ElevationRole }  from './interfaces.js'
import type { ElevationState } from './interfaces.js'

export const roles: Array<ElevationRole> = ['xs', 'sm', 'md', 'lg']
export const states: Array<ElevationState> = ['default', 'hover', 'pressed', 'disabled']
